[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'uvicorn main:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 120 --timeout-graceful-shutdown 30'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
